import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  root: { flex: 1 },
  scroll: { flex: 1 },
  page: {
    maxWidth: 720,
    gap: 16,
  },
  content: {
    paddingTop: 8,
  },
  row: {
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 8,
  },
  sectionCard: {
    marginTop: 8,
  },
});
